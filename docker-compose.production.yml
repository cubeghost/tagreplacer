version: '3'

services:

  web:
    image: 'cubeghost/tagreplacer:latest'
    volumes:
      - /home/.env:/usr/src/app/.env
    expose:
      - "4000"
    environment:
      - NODE_ENV=production
    command: npm run start
